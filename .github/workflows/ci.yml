name: Caller Workflow

on:
  workflow_dispatch:

jobs:
  call-reusable:
    uses: ./.github/workflows/wf.yml
    environment: dev
    with:
      person_name: ${{ vars.TEST_NAME}}
      environment: "dev"
